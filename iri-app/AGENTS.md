# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Dieses Projekt läuft bewusst auf Expo SDK 54 (Expo-Go-Kompatibilität auf Saschas iPhone) — kein SDK-Upgrade ohne Rücksprache.
